import { FunctionComponent } from "react";
import styles from "./TopBanner.module.scss";

const TopBanner: FunctionComponent = () => {
  return <div className={styles.wrapper}>TopBanner</div>;
};

export default TopBanner;
